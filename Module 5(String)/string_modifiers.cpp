#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin>>s;

    // s.push_back('A');
    // s.pop_back();

    // s.erase(0,3);
    // s.replace(5,0,"So");
    s.insert(5,"So");
    cout<<s;
    return 0;
}